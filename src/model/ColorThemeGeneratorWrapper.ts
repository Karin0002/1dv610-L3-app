import {
  AnalogousThemeFactory,
  ComplementaryThemeFactory,
  MonochromeThemeFactory,
  SplitComplementaryThemeFactory,
  TriadicThemeFactory
} from 'color-theme-generator'
import { Options } from './Options.js'

class ColorThemeGeneratorWrapper {
  getColorThemeOptions (): Options {
    // randomize what theme is generated
    // create an Option for the correct theme
    // create 4 Option for the incorrect themes
    // create an Options with the created Option objects
    // return Options
  }
}
